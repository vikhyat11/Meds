spring:
 datasource:
   driver: com.mysql.cj.jdbc.Driver
   url: jdbc:mysql://localhost:3306/expensecodemodule
   username: root
   password: tiger
 jpa:
   show-sql: true
   hibernate:
     ddl-auto: update
   properties:
     hibernate:
       dialect: org.hibernate.dialect.MySQL5Dialect